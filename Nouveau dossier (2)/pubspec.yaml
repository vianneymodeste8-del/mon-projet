name: task_manager_cli
description: >
  Application CLI de gestion de tâches en Dart pur (sans Flutter), avec
  priorité, date limite optionnelle, et persistance JSON locale.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Aucune dépendance externe : 100% Dart standard library.

dev_dependencies:
  test: ^1.24.0
